def text_to_sequence(text, cleaners):
    # Minimal stub: returns a list of character indices for the input text
    # Replace with actual text processing logic for VITS
    return [ord(c) % 256 for c in text]
